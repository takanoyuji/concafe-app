services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # キャスト画像・DBの永続化（コンテナ再作成しても消えない）
      - app_data:/data
    env_file: .env
    restart: unless-stopped

volumes:
  app_data:
