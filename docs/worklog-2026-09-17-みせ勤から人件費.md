# 作業ログ 2026-09-17 — 給与計算の人件費を みせ勤 から作る

| 内容 | コミット | イメージ |
|---|---|---|
| 人件費の出どころに「みせ勤」を追加 | `dee4615` | `concafe-app-app:misekin-wage-20260917` |

## きっかけ

9/17 に店長から「基本給が入っていない」。調べると、アップロードした人件費CSV（エアシフト）が
9/1〜15 の全期間分になっておらず（店全体の労働時間 63:00、普段は250時間前後）、数日分しか入っていなかった。
CSVの手作業に依存している限り同じことが起きるので、自社の勤怠アプリ みせ勤 から直接作れるようにした。

## 作り

- `lib/misekin.ts` … attendance API を全ページ取得し、キャストコード別に
  基本給 = Σ round(実労働分/60 × その日の時給) / 通勤手当 = Σ 交通費 / 労働時間 = Σ 実労働分 にまとめる
- `lib/salary.ts` … `calculateSalaryFromRows()` の人件費入力を `ArrayBuffer | MisekinWages` にした。
  CSVはエアシフト氏名、みせ勤はキャストコードで引く。計算式は変えていない（テストで CSV と同値を確認）
- `app/api/admin/salary/route.ts` … `wageSource=misekin` のとき CSV 不要。`MisekinError` は 400（データ不備）/ 502（接続）
- `app/admin/page.tsx` … 「人件費の出どころ」ラジオ。`.env` に設定があるときだけ出す（`/api/admin/salary/history` が `misekinConfigured` を返す）。既定はCSV
- 名寄せは **みせ勤の社員コード ＝ castCode**。表示名では突き合わせない（「月瀬透」vs「透」の揺れがある）

### 止める条件（黙って0にしない）
- 時給が未登録の打刻がある → 400 で該当者を列挙
- 退勤打刻が無い打刻がある → 400 で日付と名前を列挙
- 期間に打刻が0件 → 400
- 社員コードがマスタに無い人 → 計算から外して画面に警告（CSVで氏名が合わない人と同じ扱い）

## みせ勤側にやったこと（本番DB直接。`/opt/apps/misekin/backups/staff_apikeys_20260917.sql` にバックアップ）

- 星狼3店のスタッフ **28人の社員コードにキャストコードを投入**（退職済みの「月瀬」は空のまま）
- APIキー `xinglang-salary`（`mk_live_W3AAK4Wi…`、店舗スコープ＝星狼東京/大阪/名古屋、読み取り専用）を発行。
  concafe-app の本番 `.env` に `MISEKIN_API_URL` / `MISEKIN_API_KEY` / `MISEKIN_STORE_ID_{TOKYO,OSAKA,NAGOYA}` を追記（`.env.bak-20260917` に変更前を保存）

## ⚠️ まだ使えない — 時給と交通費が みせ勤に1件も無い

星狼29人とも `wage_histories` / `transportation_histories` が0件。今の状態で「みせ勤」を選ぶと
3店とも「時給が登録されていない人がいます: …」で止まる（本番APIで確認済み）。
**みせ勤のスタッフ設定で時給（必要なら交通費）を入れるまで、実運用はCSVのまま。**

打刻の普及もまだ途中（東京 9/3〜9/16 で32件。エアシフトの同期間は約250時間分）。
切り替えるときは、同じ期間を CSV と みせ勤 の両方で計算して基本給を突き合わせてから。

## 確認

- `tsc --noEmit` OK / vitest 160件（8件追加）OK
- 本番DBのコピー＋本番みせ勤APIで新イメージを別ポート起動し、ADMINセッションで
  3店 × 9月前半 × wageSource=misekin → 400「時給が登録されていない人がいます」/ wageSource=csv でCSV無し → 400「wageCsv は必須」
- 本番反映後: トップ200 / 未認証403 / コンテナに MISEKIN 環境変数5本 / クライアントJSに「人件費の出どころ」

---

## 追記（同日午後）— 時給はランク制度表、通勤手当はマスタの日額 × みせ勤の出勤日数

| 内容 | コミット | イメージ |
|---|---|---|
| ランク表に時給・交通費○/×、マスタに通勤日額 | `60773d7` | `concafe-app-app:rank-wage-20260917` |

### 経緯

「ランクから時給・交通費を取れないか。交通費を払う人はエアシフトから」と依頼。
エアシフトCSVの時給を現在のランクと突き合わせると **同ランクで1,200/1,300が混在、名古屋はブロンズ>ゴールド** で、
実績ベースでは「ランク→時給」が成り立たなかった。代表に確認したところ、**制度表**が出てきたのでそれを正にした。

| ランク | 時給 | バック率 | 交通費 |
|---|---:|---:|:-:|
| プラチナ | 1,400 | 50% | × |
| ブラック | 1,300 | 40% | × |
| ゴールド | 1,200 | 35% | × |
| シルバー | 1,200 | 30% | ○ |
| ブロンズ | 1,100 | 25% | ○ |
| 研修 | 1,100 | 20% | ○ |
| 店長 | 1,500（表に無い。エアシフト実績） | 50% | × |
| 内勤 | **0（未設定）** | 30% | ○ |

エアシフトにAPIは無いので、通勤手当は「エアシフトから取る」代わりに **キャストマスタに日額を持ち、みせ勤の出勤日数と掛ける**。
初期値はエアシフトCSV（2026/2〜6月）の実績（人ごとに1値で安定していた）14人分を投入。

### 作り
- `CastRank.hourlyWage` / `CastRank.commutePaid`、`Cast.commuteDaily`（migration `20260917000000`）
- `lib/misekin.ts`: 基本給 = Σ round(実労働分/60 × ランクの時給)、通勤手当 = 日額 × 打刻のあった営業日数（同日2打刻は1日）。
  みせ勤側の hourlyWage / transportationAmount は使わない
- `lib/salary.ts`: 通勤手当0の判定を `commutePaid` で行う。ランク表に無い名前のときだけ従来の固定リスト
- 時給0（未設定）のランクの人に打刻があれば止めて列挙。内勤がこれに当たる（大阪で やっぴー・ぎんこて）
- 管理画面: ランク管理に時給・交通費、キャストマスタに通勤日額。CSV入出力にも列を足した（無ければ現在値を引き継ぐ）

### 確認（本番DBコピー＋本番みせ勤API、9月前半）
- 東京: 200。労働時間 142:54、暗本 35,513 / Mioto 32,980 / ARIA 6,340（317分×1,200 ✓）
- 名古屋: 200。労働時間 148:45、あまね 通勤 7,000（1,000×7日）
- 大阪: 400「時給が設定されていない: やっぴー（内勤）、ぎんこて（内勤）」← 想定どおり
- vitest 165件 OK

### ⚠️ 残り
- **内勤の時給を決める**（0のままだと大阪は止まる。エアシフトでは0で運用されていた）
- **通勤日額が未入力の人**: 新しめの ルシエル・楓羽・伊藤（名古屋）、りゅう・YUN・Tag値（東京）など、CSVに実績が無かった人
- **最低賃金**: 東京 1,226円（2025-10〜）に対し、制度表のゴールド/シルバー 1,200・ブロンズ/研修 1,100 は下回る。大阪 1,177・愛知 1,140 に対しても 1,100 は下回る。
  エアシフトの実績（東京の最低が1,200）も同じ状態だったので今回始まった話ではないが、ランク表を正にした以上、値の見直しは代表判断
- 切替は同期間を CSV と みせ勤 の両方で計算して基本給を突き合わせてから
