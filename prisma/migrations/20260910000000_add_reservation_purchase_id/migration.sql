-- 予約に「遠隔ドリンクの購入ID」を持たせる（2026-09-10 決定）。
--
-- 遠隔で買ってくれた人を承認で優先するために、お客様が自己申告した BASE の注文IDを残す。
-- **システムは照合しない。** concafe-app は BASE の API を持っていないため、
-- 他人の注文IDを書くこともできる。台帳では「未照合」と明示し、
-- 購入の事実として扱う前に店舗が BASE の管理画面で突き合わせる運用にする。
--
-- 既存行は空文字（この列ができる前の申し込み）。
-- 追加のみで既存データには触らないため ADD COLUMN で足す。
ALTER TABLE "Reservation" ADD COLUMN "purchaseId" TEXT NOT NULL DEFAULT '';
